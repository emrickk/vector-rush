# Candidate 02 source and geometric evidence

`01-night-tower-groups-studio.png` is an actual Blender Cycles source inspection render: 1200×1050, 16 samples, four CPU threads, denoising off. It confirms the authored grouped-floor material assignment. It is not a native lighting or visual acceptance image.

`projection-and-light-audit.json` uses the actual candidate-01 camera transforms to project the changed station and new approach group, and checks all four light housings against the complete course. It explicitly retains viewport cropping and unrecorded imported-child orientation as native review items.

Complete placement audit and unchanged-geometry tower provenance are in `SourceAssets/environment-v3/pass-03-city-correction/`. The initial failed source-selection attempt is retained in the sibling build log; applying the imported coordinate transform before floor selection fixed the recipe, after which the source and studio image completed.

The parent owns subsequent native captures and independent acceptance. No native build or playback was started by the environment author.
