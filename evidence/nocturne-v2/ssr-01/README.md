# Native SSR feasibility 01 — in progress

Owner authorized Nocturne V2 implementation. Current control app remains `Builds/Vector Rush.app`, GUID `40bed5f53c2449418e7fb56bf59739f6`; all 189 app-file hashes match the retained control manifest. Source baseline is `ac6992c` on `build/first-playable`.

The candidate is a separately named `Builds/Vector Rush-SSR-preview-01.app`. Runtime `-vrRoadSSR on|off` controls only the reflection volume. No argument means off. Scene geometry, normals, material values, lights, craft, physics and camera remain locked.

Native availability, requested settings, observed profiler-pass evidence, still-image appearance, watched motion and performance are distinct checks. A requested flag or valid shader alone is not proof the SSR pass executes. Simulation-time captures are not real-time frame measurements. Actual missing gates remain unverified.

No completed candidate or visual acceptance is claimed by this setup record.
