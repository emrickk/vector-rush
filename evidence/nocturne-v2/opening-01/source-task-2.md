# Task 2 report — rough construction ready for native review

Owned edits: `UnityProject/Assets/Scripts/World/NightDistrict.cs`; `SourceAssets/environment-v6-context/README.md`, `audit_opening.py`, `placement-audit.json`; this report. No new Unity script asset, so no Unity `.meta` addition is required. No other runtime files, editor/project settings, native apps, commits, or Unity execution were touched.

Implemented two connected groups using actual .200/.275 pier feet and the existing .20 industrial/grid frontage. Three rough module types; 49 boxes / 588 triangles, up to eight material-batched renderers; four added housed shadowless spots; zero new textures/materials/colliders. All new geometry, footprint collection and lights are gated under `ConstructionEnabled`. Mesh ownership uses existing NightDistrict lifecycle. Original reservation methods and original off-mode content remain unchanged.

The requested source/coordinate audit and regeneration recipe are complete in `SourceAssets/environment-v6-context/README.md`. Recorded SSR02 .15/.22/.30 poses and original full-frame PNGs were inspected along with target A. Actual rejected grid locations were eliminated before authoring. Initial source projection put the second low platform behind the .22 barrier, so its finalized return uses the existing tower's 22 m north-blade contact and the .275 foot. These are pre-render placement corrections, not claims of native visual success.

Final source-computed .15 rectangles: court X0–740/Y648–919 (9.66% of frame rectangle), return X0–343/Y430–720 (4.79%). .22 return X0–680/Y639–1080 (14.44%); room alone X168–520/Y678–998. These rectangles ignore occlusion; actual visible top/front/recess gain must be judged in native frames. Both groups are behind .30/.37/.43, preserving those as regressions rather than forcing new clutter ahead.

The separate low-volume clearance test preserves landmarks, benchmark plots and existing city footprints, then checks all 1,200 course samples against a conservative banked-underbody/camera envelope. This explicitly allows a structure below the elevated road to touch its real support while preserving >22.90 m / >25.94 m additional vertical gap below that protected envelope. Existing Clear's 41 m lateral-only rule cannot allow a real support contact, so it was not weakened globally. Expected telemetry groups=2 / addedLights=4, with each group's bounds, exact foot/socket, count and minimum gap. Any failed reservation skips the complete group with a reason.

Checks performed: numerical recipe executed successfully; Python syntax compiled; `git diff --check` clean for the owned runtime file; manually checked box/column contacts, socket index handling, no native field allocations, ownership and off-mode gate. Actual Unity compilation, runtime reservation success, native lighting/occlusion, motion and performance remain for the integrator. Four extra lights are explicitly provisional; no performance or visual pass is claimed.
