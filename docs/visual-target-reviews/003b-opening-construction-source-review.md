# Opening construction — independent Task 2 source review

**Verdict: no blocking source defect found. The candidate can proceed to the integrator's native build and capture. Appearance, runtime reservation success and performance remain unverified.**

Reviewed the frozen `44aded2..330bb84` diff against Task 2's brief, the opening-finish global constraints, its source report and `SourceAssets/environment-v6-context` recipe/audit. Focused external callsite checks covered WorldBuilder's construction order and support transforms, the existing grid tower/frontage generation, transit material initialization, reservation interfaces and NightDistrict's batching/cleanup. Tasks 1/3 have a separate review in `003a-opening-source-review.md`.

No Unity process, test suite, numerical recipe rerun, screenshot capture or benchmark was started. No runtime code or source asset was modified. This source review did not independently inspect candidate images and does not establish a visual pass.

## Source assessment

- **Actual support lookup is viable.** WorldBuilder creates the supports before adding/building NightDistrict on the same GameObject. `GetComponentsInChildren<Transform>()` therefore reaches the original `Pier foot` objects. Progress .200 and .275 both correspond to the existing five-row support interval in the 200-step loop. The one-metre horizontal nearest-foot threshold selects the intended existing support. Failure skips the group before adding its meshes or lights.
- **The foot socket uses the instantiated transform.** `TransformPoint(-.33,.46,0)` incorporates the actual 11 × 2 × 12 m foot scale and its unbanked support rotation. The socket column's bottom remains at that contact when the second group is vertically scaled. The recipe adds intermediate support feet, but does not add a second foundation at either existing pier foot.
- **The contact frontage is supported by current source.** The industrial anchor is saved only after the original clearance/acceptance branch creates it. The current inner-grid generation produces the co-located tower at (152,0,-56), rotated 90 degrees, with the full-height blade terminating at Z=-37.82. That agrees with the second group's 22 m contact datum. The proximity exemption in `OpeningClear` is narrow enough to cover these two existing co-located contact buildings in this fixed candidate; it does not disable landmark or transit reservations.
- **The full-course clearance path is separate and conservative for these volumes.** Every preflight piece receives landmark and benchmark checks, then a 1,200-sample check against all nearby course branches. Horizontal distance is taken from each world AABB, not only its centre; the vertical test uses the entire AABB top and a bank/underbody/margin allowance. The one-sample padding addresses gaps between samples. Original `Clear` and reservation functions remain unchanged. Additional city overlap checks use oriented footprint separation and vertical intervals.
- **Off preserves existing construction.** Obstacle collection has an internal preview gate, and group creation is called only when `ConstructionEnabled` is true. The added frontage assignments store managed values without changing the original geometry or random-number sequence. No new camera, physics, collider, fog, exposure or race edits appear in this task.
- **Counts and ownership agree with source.** The two recipes have 19 and 22 preflight boxes, plus four housing/lens boxes each: 49 boxes and 588 triangles. Four shared material references yield up to eight new combined mesh renderers; four new shadowless spotlights exclude racer layer 8. Transit materials are initialized earlier regardless of authored-resource availability. Combined meshes enter the existing `meshes` ownership list, lights are child objects, and no new materials or textures require separate ownership. The three outdoor fixture shadow-casting exclusions are outside this diff and remain intact.

## Remaining limitations for native review

1. **Source projection is not runtime placement acceptance.** The Python audit reproduces the proposed geometry, foot coordinate, road-envelope gap and recorded-camera projection. It does not run NightDistrict's full landmark/benchmark/city reservation sequence or the actual foot search. Require `VECTOR_RUSH_OPENING_CONSTRUCTION groups=2 addedLights=4` and both accepted group records before treating the capture as the intended candidate. A skipped group is a failed candidate setup, not evidence that the construction had no visual benefit.
2. **Attachment and visibility require rendered observation.** The fixed north-blade coordinate and contact exemption are correct for the reviewed existing city, but are coupled to that source recipe. The large unoccluded screen rectangles cannot prove visible top/front/recess area behind the road barrier or piers. Check both contacts, the open span intervals and .15/.22 composition directly; keep .30/.37/.43 as depth/clutter controls. No broader rollout is justified by this source review.
3. **Light leakage and cost remain open.** The four lights have explicit housings and supported source positions, but shadowless cones with ranges 36/58 m can light nearby surfaces through intervening construction. Their broad effect and effect on the road need the native construction-only/combined comparison. Reported light counts and source culling masks do not establish frame-time cost.
4. **The clearance audit is not a universal future geometry validator.** Existing city footprints are conservative approximations for recorded building families, and the actual small lamp housing/lens boxes are emitted after the `pieces` preflight. They sit within the current group's coarse bounds, with substantial reported road clearance, so no present blocking overlap follows from this. Any later lamp or frontage movement must update/check the real emitted geometry rather than assuming the existing preflight covers arbitrary additions. The existing source audit also needs regeneration when its recorded runtime inputs change.

The next gate is the separate native build with actual group telemetry, followed by an independent whole-frame verdict and continuous opening observation. A passing compile or CPU/source check does not resolve these appearance and motion risks.
