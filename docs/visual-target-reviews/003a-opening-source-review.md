# Opening preview Tasks 1 and 3 — independent source review

**Verdict: no blocking source defect found in the reviewed candidate. Proceed to the integrator's native compile/build and comparison gates. This is not a visual, playability, motion or performance pass.**

Reviewed candidate `44aded2` against `2908112`, using the supplied review diff, Task 1/3 briefs, opening-finish plan/global constraints and both source reports. Reviewed Task 1's nine-anchor addendum and the accompanying sparse validator. Task 2's uncommitted `NightDistrict` work is excluded. No Unity, build, test suite, capture or benchmark was run. No candidate image was independently inspected in this source review; the implementer's appearance claims remain hypotheses.

## Source findings

No actionable blocking bug was identified. The implementation matches the bounded source contract:

- `OpeningFinishPreview` uses managed command-line parsing, defaults missing/invalid values to off, exposes both required gates and logs the selected state once at bootstrap. The changed paths add no camera, race or physics tuning.
- The preview build rejects an existing destination and the original app path before calling `Prepare()` once. It selects Solstice/StandaloneOSX, Instrumented managed code and `BuildOptions.None`, disables the installed SSR feature, checks the default renderer/SSAO state and throws on build failure. The existing SSR volume configuration does not reactivate the disabled renderer feature. Prior SSR source remains intact.
- Opening stills retain 1,440 pose records, use the nine sorted thresholds and unique labels, and request PNGs only for those anchors. In this mode `frame.file` equals the actual flat `anchorFile`; the ordinary evidence path remains intact. Completion/reference loops use the actual anchor count. The sparse validator checks completion, crossings, dimensions, PNG integrity, count, pose deltas and preserved source/app identity. Pose mismatches are reported rather than silently passed as exact matches.
- The new road partition agrees with `WorldBuilder.Ribbon`'s 960 segments, 12 transverse columns, triangle winding and UV direction. The unchanged renderer excludes exactly the selected rows; the two regional meshes copy source vertices, normals and tangents. The original complete mesh stays on the original collider. New meshes/textures have owner cleanup, and new materials use WorldBuilder ownership.
- `TrackPath.Evaluate` uses distance-normalized progress, so the atlas's longitudinal coordinate is meaningful in metres. The regional materials retain the actual RoadSurface template's normal/metallic-map keywords, environment-reflection opt-out, .90 smoothness multiplier and .25 normal strength. Their nominal mask endpoints remain .35–.50. There is no camera-dependent highlight painting or new reflection claim.
- Opening overrides affect only the twelve existing outdoor lights at indices 7–18. The origins match their existing diffuser centres with a small vertical offset; intensity, aim and cone changes provide a substantive source study. The three exterior fixture combinations retain `ShadowCastingMode.Off`. Warm-gallery changes retain its six bays and eighteen lights, use material separation without invalid gallery normal-map UV assumptions, and leave cool-gallery construction on its original path.

Focused callsite checks covered the road mesh/material contract, arc-length mapping, gallery loop bounds, `VectorRushSetup.Prepare`, the retained RoadSurface material and SSR startup configuration. No broader code audit was performed.

## Native acceptance risks, not established defects

1. **Road-region transitions need direct observation.** `OpeningRoadFinish.cs:95` blends toward samples from the original level-zero control arrays, then stores those samples in a much coarser longitudinal atlas. This preserves neither the original texture's sampling footprint nor mip identity. Matching boundary vertices prevents a geometry crack, but does not prove a seamless albedo/normal/smoothness transition. Inspect near progress .114583, .340625, .859375 and .902083 in ordinary racing motion; the nine requested still anchors alone do not cover every seam. Reject visible bands or shimmer rather than inferring continuity from the source fade.
2. **Visible improvement remains unproven.** The narrower opening cones and redistributed peaks may leave useful road sections dark; warm-gallery face/rib contrast may be too strong. The bounded intensity increases and material variation support a plausible comparison, but cannot establish clearer whole-frame depth or useful broad surface response. Compare off/surface/combined at the ordinary opening and warm-gallery views, then check .37/.43, thermal and station controls. Direct light response still does not supply reflected fixture/building structure.
3. **Build preservation checks have a limited scope.** The SSAO snapshot is taken after `Prepare()`, which itself writes the known AO configuration and activates it. The checked-in renderer currently matches those values, so this is not a blocker for this candidate. The log should not be treated as proof that arbitrary pre-existing renderer settings survived unchanged; the integrator still needs the generated-settings diff and same-binary off comparison required by the plan.

The separate native build, nine-anchor/end-status validation, independent image verdict, continuous transition observation and isolated performance remain pending with the integrator. CPU/source checks cannot substitute for any of those gates.
