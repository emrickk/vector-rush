# Live coordination

Candidate-03 native race verification is COMPLETE: three races, all six finish, zero recoveries, stable results. The architecture artist now owns the heavy-work slot for Blender studio renders/export self-checks. Rendering worker remains source-only; parent will not run Unity/build/native while that art slot is held. Artist must state when the slot is released in its handoff.

Critic 001 remains a valid blocked record because that Astra session could not receive images. A project-scoped `visual_critic` agent now selects image-capable `gpt-5.6-sol`; a synthetic PNG plus required function-tool call passed through the configured Unity LiteLLM provider. No Vector Rush image has yet been inspected through that route. Source/modeling work may proceed into staging, but no replication or promotion into the original game's foreground occurs until a fresh critic uses the Sol agent on the named originals and records an observed verdict. The next handoff must distinguish completed technical assets from visual review status.

Original art is the baseline. Direct-production candidate is owner-rejected. Keep four-zone game ambition, but do not replace it with a box kit or claim a source implementation solves art quality. Parent will integrate optional staged content only after the appearance can be checked.

Parent compiled the original-baseline hook after adding folder metadata and bootstrap call; all 63 tests pass. Rendering handoff is complete. Gameplay correction worker owns RaceFinishLedger/RaceDirector/RaceRecords/ProductionSettingsUI/PlayerPreferences/RaceHUD plus new AAAInteractionTests; don't edit those paths concurrently. Artist may finish export/reimport checks and handoff; no visual adoption is allowed.

Unity 6000.6.0f1 is now installed. Parent will use isolated `Builds/AAAValidationProject` with original pinned packages for next tests/import/native builds, not the earlier 6000.5 compatibility copy. Art package STAGED payload is published and parent verified 29 hashes. No integration/render acceptance is implied. Import worker owns only `AAAExemplarImport.cs` and meta plus its handoff; no other lane edits that file.

GAMEPLAY CORRECTION FEEDBACK: parent ran the current source/test snapshot under Unity 6000.6. `evidence/aaa-rebuild/corrections/tests-01.xml`: 106 total, 91 passed, 3 failed, 12 skipped. Failures are AAAInteractionTests.IMGUIKeyActivationIsConsumedBeforeFocusedButtonsCanReplayIt for Return/Space/KeypadEnter, expected true but false. This is not a pass. Inspect report and resolve test-vs-real-IMGUI event context; do not suppress failures or count skipped keyboard coverage as pass. Final source needs a fresh parent run. Importer not yet included in that run.

## Current visual-first pass (supersedes slot/status notes above)

Three fresh Sol subprocess workers are active under `gallery-02/dispatch.md`: artist owns new gallery02 package/recipes and heavy Blender slot; integrator owns AAA Unity code plus NightTrackLighting source only; independent critic owns gallery02 critique reports. Parent owns EnvironmentEvidence gallery capture mode and local validator, test/build/capture execution after Blender slot release. No gameplay lane is active. Sol images worked in critic002; current artist/critic receive the same owner-approved original warm-gallery and generated target files. Gallery02 importer contract is published in `gallery-02/integration-handoff.md`; artist must read it before finalizing. Existing uncommitted Solstice serialization IDs and TimeManager changes predate this pass and remain unadopted.
