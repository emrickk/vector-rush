# Gallery visual production ownership

Owner authorizes visual-first implementation with subsystem workers. Parent integrates and records native evidence; independent Sol critic decides whether the integrated section earns adoption. Original track, craft, HUD, camera, global atmosphere and continuous warm-gallery shell remain controls. Direct-production architecture remains rejected. Warm gallery spans progress .86 to .902 (about 77 metres). Finish its complete connected section to avoid a short high-detail patch that cannot integrate with the combined original meshes.

Next bounded step before implementation: revise the Blender package from the current pinned course export, author coherent portal/ribs/wall cassettes/ceiling fixtures with prepared materials and an explicit fixture contract; integrate optional whole-warm-gallery replacement on the original world including local lighting; build and capture matched native control/candidate and approach/interior/exit sequence; independent visual critique and correction before adoption. Original resources and rejected artifacts are preserved. Parent's image tool is unavailable, so visual critic inspection is mandatory. Whole-circuit production follows only after exemplar quality, not in this first gallery milestone.

- Artist owns `SourceAssets/aaa-nocturne/recipes/*gallery02*`, new `SourceAssets/aaa-nocturne/exemplar-01-gallery02/`, and `docs/aaa-rebuild/gallery-02/art-handoff.md`. Can run Blender generation/exports. Holds heavy GPU slot until handoff; parent does source work meanwhile. Do not overwrite exemplar-01.
- Unity integrator owns `UnityProject/Assets/Editor/AAA/`, `UnityProject/Assets/Scripts/World/AAA/`, `UnityProject/Assets/Scripts/World/NightTrackLighting.cs`, new `UnityProject/Assets/Tests/Editor/AAAGallery*`, and `docs/aaa-rebuild/gallery-02/integration-handoff.md`. Source only; parent runs Unity after art heavy slot released. Integrator and artist communicate contract through their handoffs; parent dispatches followups as needed.
- Independent critic owns only `docs/aaa-rebuild/gallery-02/critic-*.md`. First assess concrete construction/light brief using approved original/reference images; later inspect actual current native captures. No implementation, git or other lane edits.
- Parent owns evidence harness, validation copy/build/captures, current plan/history, coordination and commits. No native/editor work while Blender heavy slot held. No gameplay tuning in this milestone.

Workers may not spawn additional workers, change credentials, publish/commit, or overwrite other lanes. Native/build evidence is local; temporary captures and process logs stay out of commits. Source, Blender and exported assets retain metadata/checksums. Native inspection determines appearance, tests determine technical contracts, no AAA/perfection claim follows from either alone.

Parent integration note: the Unity worker's newly published handoff defines gallery-only package import and canonical float-bit identity with separate exact source-byte provenance. This supersedes the initial prompt's assumption that opening resource placements must be carried into the new gallery package. Artist and integrator must use the published handoff as shared schema; preserve the old opening package separately. There are currently no published AAA resources in source because the prior import failed. Therefore the gallery review builder must permit an absent opening resource, require the new gallery, and not depend on the old source package importing first. Critic brief is now available at `docs/aaa-rebuild/gallery-02/critic-brief.md` for both implementation lanes.

Canonical identity cross-check: parent independently packed both old and fresh JSON exports as `VRCRS1`, little-endian int32 sampleCount1200, width/length float32, then each frame's progress,distance,position,forward,right,up float32. Both produce `8c2161b930d43c505c8380ce30a719e6833a453d671353994f79b75e4c67a79e`. Parent owns the existing AAAImportContractTests identity assertion update; integrator owns new AAAGallery tests only. This preserves the serialization failure record while testing the intended stable identity.

Parent native evidence tooling: `-environmentGallery` records288 actual native simulation frames at24fps with nine natural anchor crossings (.83,.85,.86,.868,.878,.888,.898,.91,.93). It is automated steering and simulation-time silent footage, not human driving or a performance sample. Control/candidate reports record mode/build identity and existing camera/vehicle comparisons. Validator rejects corrupt/missing images, wrong dimensions or mismatched pose acceptance. Comparison HTML/contact sheets link or retain the actual original anchors; contact sheets are review aids, not replacement evidence.

Rendering integration caution: current VectorPipeline.asset has `m_AdditionalLightShadowsSupported: 0`. A fixture with LightShadows.Soft alone does not establish shadowed pools in this pipeline. Source/native report must distinguish requested fixture shadows from actual pipeline support. Preserve off-state defaults; if local-shadow support is required, parent can use a separately validated review pipeline shared by control/candidate and record its effect. No unobserved claim of contact-shadow improvement.
