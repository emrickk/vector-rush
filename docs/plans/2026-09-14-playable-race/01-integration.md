# Handoff 01: integration and delivery

Use **GPT-5.6 Sol**. You are the integration owner for Vector Rush. The user authorized executing the polished-race plan and will start three other conversations from the sibling handoffs. Do the work, not another broad planning exercise.

Project: `/Users/anping.wang/Documents/Stuff/AI Space/Vector Rush`.
Packet: `/Users/anping.wang/Documents/Stuff/AI Space/Vector Rush/docs/plans/2026-09-14-playable-race`.

Read `README.md` in this packet, root `AGENTS.md`, `docs/aaa-rebuild/assessment-2026-09-09.md`, and `docs/aaa-rebuild/gallery-02/integration-handoff.md`. This new owner-approved packet selects Sol for all roles. Historical claims of active workers or completed validation are not current evidence.

## First assignment: prepare safe parallel work

1. Inspect the checkout, current index, remotes, app identities and running Unity/Blender processes without changing them. The last read found no HEAD commit and extensive staged/untracked work. Do not reset, clean, stash everything, check out another branch in place, or blindly commit the current index.
2. Preserve the original source, editable assets, metadata, Git state and native baseline in a recoverable snapshot. Exclude disposable caches from source copies, but do not delete them from the original. Inventory potential secrets privately before any commit or push.
3. Establish a private local integration copy and three sibling lane copies from one recorded snapshot. Use worktrees only if a valid committed baseline exists; otherwise make isolated copies and establish a local source baseline there. Do not rewrite the original Git history or index. Document provenance and excluded caches.
4. Write `coordination.md` in the shared packet with baseline identity, absolute workspace paths, start status, lane ownership, report locations and a simple atomic heavy-process lease protocol. Never remove another live worker's lease or kill user processes.
5. Inspect current race APIs and write the minimal `contracts.md` for Racing and Replay. Avoid a large abstraction rewrite.
6. Mark `WORKSPACES_READY` and tell the user they can launch handoffs 02, 03 and 04. Continue baseline/native work independently. Do not spawn substitutes for these user-created conversations.

## Continuing responsibility

Own shared bootstrap, scene/ProjectSettings, assembly/build changes and shared evidence tooling. Keep root implementation plan and development history current. Workers own only their assigned lanes. Read their reports when resumed; do not assume they are running or finished from old notes.

Preserve the original night composition; the direct-production art is rejected. Adopt useful gameplay changes by evidence, without replacing the world wholesale.

Verify the gallery course-identity reconciliation using actual package/current source data. Import only validated resources. Own final native baseline/candidate captures and integration. Help the artist iterate substantive defects. Once the gallery passes, release the full-course art milestone; it remains required scope.

Integrate Racing's minimal hooks before Replay's final connection. Resolve interfaces yourself or return a precise request to the responsible worker. Do not silently overwrite either lane. Keep ghost/sector timing, race progression and records on the same authoritative clock and compatibility identity.

Run suitable regression tests, full native races, pause/restart/settings checks, recorded audiovisual review and isolated performance sampling. Existing 114-test and direct-03 records are historical. Do not use a simulation-time capture as a frame-rate measurement.

Inspect the actual native images yourself. Watch/listen when the tools support that; otherwise leave that criterion open. Human handling and physical gamepad comfort require human/hardware evidence. Prepare an owner-ready build and concise driving checklist rather than declaring those passed.

## Deliverables and exit

- Shared `coordination.md`, `contracts.md`, and `reports/01-integration.md`.
- Recoverable source baseline and separate lane workspaces.
- Integrated source/history, identified native app and full-race evidence.
- Five-person playtest sheet with observation fields, not fabricated answers.
- Owner report: what improved, remaining limitations and exact app to launch.

Do not publish a release, contact testers, purchase assets, merge main or create automations. Push only scoped completed work to the established authorized private repository when access is verified; if blocked, preserve local history and report it without switching accounts or remotes.

At an external dependency, finish independent authorized work and give the user the exact follow-up needed. Do not promise background monitoring after ending the turn.
