# Apex application icon

Owner-selected option 2 for Vector Rush, September 17, 2026. Generated with GPT Image 2 through Scenario. The exact prompt and generation job identity are included; no service credential or network dependency is required.

`vector-rush-icon-1024.png` is the approved master. PNG sizes 16–1024, Windows ICO and macOS ICNS are packaged here. `UnityProject/Assets/Branding/ApexIcon.png` is an identical runtime-project copy, configured as the default Player Settings icon. Platform builds use that default unless explicitly overridden. Existing native app bundles need a rebuild to adopt it.
